import React, { useState, useEffect } from "react";
import rgbToHex from "./utils";

const SingleColor = ({ list }) => {
  return <div>{/* {list.map(single => {
        single;
      })} */}</div>;
};

export default SingleColor;
